# Spec Kit & Speckit Agent Skills

Reference Spec-Driven Development (SDD) framework.
Backbone for: `constitution -> specify -> clarify -> plan -> tasks -> implement`.

Integrates directly with `core.spec_kit_adapter.SpecKitAdapter` to generate `architecture-state.yaml` and `TargetEngine` targets.
